name: CI
on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: "Build and push"
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Build docker image
      run: docker buildx build -t asolheiro/app-journey:latest .